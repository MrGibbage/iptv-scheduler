# Copy to .env (in this directory) and fill in real values. Never commit .env.

PORT=4000
DB_PATH=./data/dev.sqlite

# iptv-recorder API this service calls into
RECORDER_API_BASE_URL=http://localhost:3300
RECORDER_API_KEY=replace-with-issued-client-key
